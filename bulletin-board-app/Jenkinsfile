pipeline {
  agent none
  stages {
    stage('Docker Build') {
      agent any
      steps {
        sh 'sudo docker build -t vikson36/bb-app:${BUILD_NUMBER} /var/lib/jenkins/workspace/docker_pipeline/bulletin-board-app'
      }
    }
  }
}
